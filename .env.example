# Contact form — Hostinger SMTP (Nodemailer)
SMTP_USER=info@muhammadkashif.net
SMTP_PASS=your_hostinger_email_password

# Admin login (/admin/login) — never commit real values
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=replace_with_a_long_random_value

ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD_SALT=replace_with_a_long_random_value
# ADMIN_PASSWORD_HASH = sha256(ADMIN_PASSWORD_SALT + your_password), hex-encoded
# Generate: node -e "console.log(require('crypto').createHash('sha256').update(process.env.SALT + process.env.PW).digest('hex'))"
ADMIN_PASSWORD_HASH=replace_with_generated_hash

# Vercel Postgres (Neon) — auto-injected when you connect storage in Vercel dashboard
# DATABASE_URL=

# ── Stripe — service checkout (embedded Payment Element) ───────────────
# Dashboard → Developers → API keys. Use pk_test_/sk_test_ while testing.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx

# Dashboard → Developers → Webhooks → add endpoint:
#   https://muhammadkashif.net/api/stripe/webhook/     <- trailing slash required
# Subscribe to: payment_intent.succeeded, payment_intent.payment_failed
# Then copy the endpoint's signing secret here.
# Locally: stripe listen --forward-to localhost:3000/api/stripe/webhook/
STRIPE_WEBHOOK_SECRET=whsec_xxx

# ── Geo restriction — serve the site to the US only ────────────────────
# Setting GEO_BYPASS_SECRET is what TURNS THE RESTRICTION ON. It is also your
# own way in: visit https://muhammadkashif.net/?geo-key=<this value> once per
# browser and a year-long cookie lets you through from anywhere.
#
# Enforcement is tied to this key on purpose — the site is run from Malaysia,
# so a block with no way back in would lock you out of your own admin.
# Generate: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
GEO_BYPASS_SECRET=replace_with_a_long_random_value

# Optional. Comma-separated ISO-3166 alpha-2 codes. Defaults to US.
# GEO_ALLOW_COUNTRIES=US

# Optional. Set to 'off' to disable the block without deleting the secret.
# A Vercel env change needs a REDEPLOY to take effect — not instant.
# GEO_RESTRICT=off
